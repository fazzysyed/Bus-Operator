import React, { FC } from 'react';
import { TouchableOpacity, Text } from 'react-native';
const Button = () => {
  return (
    <TouchableOpacity>
      <Text>Search</Text>
    </TouchableOpacity>
  );
};

export default Button;
